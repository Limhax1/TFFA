messages:
  prefix: "&8[&#CD0000FFA&8] &8» "
  reload: "&#CD0000Config reloaded successfully!"
  join-already-started: "&#CD0000The event has already started!"
  join-not-running: "&#FF0000There is no active game! Wait for the next one."
  join-already-in-event: "&#FF0000You already joined the game!"
  join-inventory-not-empty: "&#CD0000Empty your inventory before joining!"
  joined-event: "&fYou joined the &eFFA &farena!"
  leave-not-running: "&#FF0000There is no active game!"
  leave-not-in-event: "&#FF0000You are not in the game!"
  left-event: "&#CD0000You left the FFA event."
  start-already-running: "&#CD0000The game is already running!"
  event-starting: "&e%seconds% &#FFFFFFseconds left to join! &#AAAAAA(/ffa join)"
  event-started: "&#00FF00PvP Enabled! The game has started!"
  event-started-command: "&#CD0000Event started!"
  start-announce: "&fThe &eFFA &farena has started! &7(/ffa join)"
  stop-not-running: "&#FF0000There is no active game!"
  event-stopped: "&fThe &eFFA &farena has been stopped."
  stop-announce: "&#FF0000The game has ended!"
  player-joined-announce: "&e%player% &#FFFFFFjoined"
  player-left-announce: "&e%player% &#FFFFFFleft the game"
  event-win-announce: "&#00FF00Congratulations, &e%player% &#00FF00won the game!"
  kit-set-success: "&#CD0000Kit set successfully!"
  kit-give-success: "&#CD0000Kit given to the player!"
  kit-player-not-online: "&#CD0000Player not found!"
  kit-no-permission: "&#CD0000You don’t have permission for kits!"
  kit-no-kit-available: "&#CD0000No kit available!"
  kit-given: "&#CD0000Kit given!"
  kit-reloaded: "&#CD0000Kits reloaded!"
  command-rejected: "&#FF0000Commands are disabled during FFA! Use: &#FFFF00/ffa leave"

settings:
  # dont really change this.
  kit-size: 45
  # the world name for the event.
  world-name: "ffa"
  # time before the event starts - players won't be able to join after this countdown.
  start-delay: 10 # in seconds
  # allow people to join with an empty inventory.
  require-empty-inv-to-join: true
  # the delay between giving the player velocity when near the edge of the border.
  # higher = slower, smaller = faster
  border-velocity-cooldown: 20 # in milliseconds
  # distance outside the border before knockback triggers
  border-knockback-threshold: 0.15
  # minimum vertical knockback that should be applied to the player when knocking back from the border.
  border-velocity-y: 0.05
  # strength of the knockback given
  border-velocity-strength: 0.15
  # base multiplier applied to border knockback
  border-velocity-multiplier: 1.0
  # distance outside the border to apply the far multiplier
  border-velocity-far-distance: 2.0
  # multiplier when player is far outside (beyond border-velocity-far-distance)
  border-velocity-far-multiplier: 2.0

  # Potion effects that get applied during the event
  # Format: "EFFECT_TYPE:duration_in_ticks:amplifier_level:delay_in_seconds"
  # Duration: 20 ticks = 1 second
  # Amplifier: 1 = level 1, 2 = level 2, etc.
  # Delay: seconds after event starts before effect is applied
  potion-effects:
    - "STRENGTH:100000:3:0"        # Strength III for a long time, applied immediately when event starts

  shrink-border: true
  border-shrink-delay: 3 # in seconds
  border-shrink-size: 7 # in blocks
  border-shrink-time: 10 # in seconds
  border-original-size: 20 # in blocks
  # disable applying border knockback once the border has finished shrinking
  disable-velocity-when-border-reached-final-pos: true

  # these will run for the winner.
  reward-commands:
    - "give %player% apple 1"

  # these will run when a player joins the event.
  join-commands:
    - "spawn %player%"

  # these will run when a player gets eliminated.
  elimination-commands:
    - "spawn %player%"